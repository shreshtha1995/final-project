import { Component } from '@angular/core';

/** STUB — implemented by M5 · List a Room. See TEAM_PLAN.md. */
@Component({
  selector: 'app-create-listing',
  template: `
    <section class="card stub">
      <h2>List a Room</h2>
      <p class="muted">Coming soon — owned by <strong>M5 · List a Room (create / edit / images / expiry)</strong>.</p>
    </section>
  `,
  styles: [`.stub { max-width: 560px; margin: 48px auto; text-align: center; }`]
})
export class CreateListingComponent {}
