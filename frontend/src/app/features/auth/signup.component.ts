import { Component } from '@angular/core';

/** STUB — implemented by M3 · Auth. See TEAM_PLAN.md. */
@Component({
  selector: 'app-signup',
  template: `
    <section class="card stub">
      <h2>Register</h2>
      <p class="muted">Coming soon — owned by <strong>M3 · Auth (login / landing / register)</strong>.</p>
    </section>
  `,
  styles: [`.stub { max-width: 560px; margin: 48px auto; text-align: center; }`]
})
export class SignupComponent {}
